{
  "name": "trend-scanner",
  "version": "1.0.0",
  "private": true,
  "description": "Crypto + Forex trend & volatility scanner, secured with Google OAuth, deployable on Cloudflare Pages.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "pages:build": "npx @cloudflare/next-on-pages",
    "preview": "npm run pages:build && npx wrangler pages dev .vercel/output/static",
    "deploy": "npm run pages:build && npx wrangler pages deploy .vercel/output/static"
  },
  "dependencies": {
    "next": "14.2.5",
    "next-auth": "5.0.0-beta.20",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@cloudflare/next-on-pages": "1.13.2",
    "vercel": "37.4.2",
    "wrangler": "3.72.0"
  }
}
